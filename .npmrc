# Ensure scoped packages are published to npm registry
@use-user-media-hook:registry=https://registry.npmjs.org/

# Ensure public access
access=public

# Use npm v10+ for better lockfile
engine-strict=true
